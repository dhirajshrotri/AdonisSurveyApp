@layout('main')
@section('content')
    @if(old('notification'))
        <div class="alert alert-success">
        {{ old('notification') }}
        </div>
    @endif
    <div class="container text-center">
        <div class="row">
            <p>
                Welcome to Surveyor. Your account has been created!
            </p>
        </div>
        <div class="row">
            <button type="button" class="btn btn-link" href="/">Go back</button>
        </div>
    </div>
@endsection